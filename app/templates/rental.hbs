<!-- 
Display the single-rental component here and pass it the model (representing a single rental object) (accessible within the single-rental component by a variable named rental). We also pass to the component 2 strings that refer to actions in the rental route handler.
-->
{{single-rental rental=model updateRental="updateRental" destroyRental="destroyRental"}}