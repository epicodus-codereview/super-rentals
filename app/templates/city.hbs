<!-- 
Display the single-city component here and pass it the model (representing a single city object) (accessible within the single-city component by a variable named city). We also pass to the component 3 strings that refer to actions in the city route handler (routes/city.js).
-->
{{single-city city=model updateCity="updateCity" destroyCity="destroyCity" saveRental="saveRental"}}